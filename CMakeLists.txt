cmake_minimum_required(VERSION 3.16)
project(MovieTicketBooking)
set(CMAKE_CXX_STANDARD 17)
include_directories(include)
add_library(movielib src/Movie.cpp)
add_executable(movie_booking.elf src/main.cpp)
target_link_libraries(movie_booking.elf movielib)
enable_testing()
add_subdirectory(tests)
